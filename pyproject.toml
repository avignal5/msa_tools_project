[project]
name = "msa-tools"
version = "0.1.0"
description = "Simple utilities for multiple sequence alignment analysis"
authors = [{name = "Alain Vignal"}]
dependencies = ["numpy"]
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
