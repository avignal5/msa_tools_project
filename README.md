# msa_tools

To install:
```
pip install git+https://github.com/avignal5/msa_tools_project.git@main
```
Example scripts for use in tests

For allowing editing :
In the msa_tools_project : pip install -e .


